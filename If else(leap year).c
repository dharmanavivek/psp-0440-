#include <stdio.h>

int main() 
{
    int a;
    printf("enter the values a:");
    scanf("%d",&a);
    if(a%4==0)
    {
   printf("its a leap year");
    }
    else
    {
    printf("not a leap year");
    }
    return 0;
    }
